<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.makeapp">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="com.android.vending.BILLING" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:name=".App"
        android:theme="@style/AppTheme">

        <meta-data android:name="net.hockeyapp.android.appIdentifier" android:value="${HOCKEYAPP_APP_ID}" />

        <activity
            android:name=".ui.SplashActivity"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.signin.SignInActivity"
            android:windowSoftInputMode="stateHidden" />

        <activity
            android:name=".ui.signup.SignUpActivity"
            android:windowSoftInputMode="stateHidden" />

        <activity
            android:name=".ui.forgot.ForgotPassActivity"
            android:windowSoftInputMode="stateHidden" />

        <activity
            android:name=".ui.categories.CategoriesActivity" />

        <activity
            android:name=".ui.videos.VideosActivity" />

        <activity
            android:name=".ui.about.AboutActivity"
            android:windowSoftInputMode="stateHidden" />

        <activity
            android:name=".ui.reset.ResetPassActivity"
            android:windowSoftInputMode="stateHidden" />

        <activity
            android:name=".ui.videos.buy.BuyCourseActivity"
            android:windowSoftInputMode="stateHidden" />

        <activity
            android:name=".ui.videos.player.PlayerActivity"
            android:windowSoftInputMode="stateHidden"
            android:screenOrientation="landscape"/>

        <activity
            android:name=".ui.information.InformationActivity"
            android:windowSoftInputMode="stateHidden" />

        <activity
            android:name="com.facebook.FacebookActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/fb.key"/>
    </application>

</manifest>